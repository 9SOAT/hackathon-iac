name: Destroy

on:
  workflow_dispatch:

jobs:
  destroy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    env:
      AWS_REGION: us-east-1

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar AWS credenciais via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT }}:role/${{ secrets.AWS_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Compactar Lambda em ZIP
        run: |
          zip infra/v1/presigned_dummy.zip lambdas/python/presigned_dummy.py

      - name: Configurar Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Inicializar Terraform
        run: |
          cd infra/v1
          terraform init -reconfigure

      - name: Destruir Lambda
        env:
          TF_VAR_verified_email: ${{ secrets.VERIFIED_EMAIL }}
        run: |
          cd infra/v1
          terraform destroy -auto-approve
